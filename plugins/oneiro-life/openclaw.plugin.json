{
  "id": "oneiro-life",
  "name": "Oneiro Life",
  "description": "Records the OpenClaw gateway's own lifecycle into the Oneiro OSTIS graph.",
  "categories": ["other"],
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "dashboardUrl": {
        "type": "string",
        "description": "Loopback URL of the project dashboard that owns the OSTIS bridge.",
        "default": "http://127.0.0.1:8130"
      },
      "sessionId": {
        "type": "string",
        "description": "Session id the gateway's own records carry in the graph.",
        "default": "oneiro-gateway"
      }
    }
  }
}
