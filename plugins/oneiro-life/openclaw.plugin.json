{
  "id": "oneiro-life",
  "name": "Oneiro",
  "description": "Записывает в память Oneiro сведения о собственном запуске и остановке помощника.",
  "categories": ["other"],
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "pythonPath": {
        "type": "string",
        "description": "Программа python, которая выполняет запись в память.",
        "default": "python"
      },
      "sessionId": {
        "type": "string",
        "description": "Имя сессии работы, под которым записи помощника ложатся в память.",
        "default": "oneiro-gateway"
      },
      "ostisPort": {
        "type": "integer",
        "description": "Порт, на котором отвечает память помощника.",
        "default": 8090
      }
    }
  }
}
