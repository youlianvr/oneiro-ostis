{
  "id": "oneiro-life",
  "name": "Oneiro Life",
  "description": "Records the OpenClaw gateway's own lifecycle into the Oneiro OSTIS graph.",
  "categories": ["other"],
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "pythonPath": {
        "type": "string",
        "description": "Python interpreter that runs the recorder which writes to OSTIS.",
        "default": "python"
      },
      "sessionId": {
        "type": "string",
        "description": "Session id the gateway's own records carry in the graph.",
        "default": "oneiro-gateway"
      },
      "ostisPort": {
        "type": "integer",
        "description": "Port of the sc-machine the recorder connects to.",
        "default": 8090
      }
    }
  }
}
